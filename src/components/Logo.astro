---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---
<a href="/" class:list={["inline-flex items-center gap-0.5 text-2xl font-bold tracking-tight", className]}>
  <span class="text-primary">Lako</span><span class="text-accent">.</span>
</a>
